apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: booking-tickets
spec:
  selector:
    app: booking-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP